##  {{name}} 权限地址
```
{{route_name}}.index  列表
{{route_name}}.del  删除
{{route_name}}.add|{{route_name}}.doAdd  添加
{{route_name}}.save|{{route_name}}.doSave|{{route_name}}.status 修改
```
##  {{name}} 访问地址
```
admin/{{route_name}}/index  列表
```

